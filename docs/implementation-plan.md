# 実装ロードマップ

## 第1段階: ハードウェア基盤

**目標:** Pico WH とサーボを安定して動かす

- [ ] 5V 並列給電の配線
- [ ] GND 共通化の確認
- [ ] GPIO から PWM でサーボ制御
- [ ] ボタンを押せる角度の調整
- [ ] サーボの物理固定（両面テープ・ステー等）

**完了条件:** サーボがインターホンの開錠ボタンを確実に押せる

---

## 第2段階: ローカル操作

**目標:** 家の Wi-Fi 内からスマホで操作

- [x] Pico WH に簡易 HTTP サーバを実装 → `src/main.py`
- [x] `/unlock` エンドポイントでサーボ動作 → `src/servo.py`
- [ ] スマホのブラウザからアクセスして動作確認

**ソースコード:**
| ファイル | 役割 |
|----------|------|
| `src/config.py` | Wi-Fi・サーボ・サーバの定数 |
| `src/servo.py` | サーボ制御クラス |
| `src/main.py` | Wi-Fi接続 + HTTPサーバ + メイン処理 |

**完了条件:** 同一ネットワーク内のスマホから開錠できる

---

## 第3段階: リモート操作

**目標:** 外出先から開錠できる

- [ ] 通信方式の決定（ポート開放 or Webhook）
- [ ] セキュリティ対策（秘密キー付き URL 等）
- [ ] LINE Bot 連携（Webhook 方式の場合）

**完了条件:** 外出先のスマホから開錠操作ができる
